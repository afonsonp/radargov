flask
requests
pypdf
# desenha as paginas das pecas no servidor (visualizador proprio do
# painel, /peca): o <embed> do browser recusava abrir PDFs quando o
# Chrome esta em "transferir em vez de abrir"
pymupdf
# destranca os PDFs de anúncio cifrados com AES -- são 19 na base, e
# nenhum é Caderno de Encargos nem Programa, por isso não afecta a
# leitura das peças; é só para o PDF do anúncio abrir.
cryptography
# lê o Excel de análise de concursos da casa (--importar-excel); puro
# Python, só se importa nesse comando
openpyxl
# OCR das peças digitalizadas (2 em 12 CE/PC medidos não têm camada de
# texto): os modelos PP-OCR em ONNX, sem o framework PaddlePaddle. O
# modelo que a roda traz lê português com acentos, sem descarregar nada.
# Opcional: sem ele o radar marca 'scan' e segue, como até aqui. O
# rapidocr não declara o onnxruntime, por isso vai aqui nomeado. Puxa
# opencv, numpy, shapely e omegaconf (que compila o antlr4 4.9 do código
# fonte: é Python puro, mas se a compilação falhar repete com
# --no-build-isolation).
rapidocr
onnxruntime
